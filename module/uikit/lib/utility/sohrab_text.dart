import 'package:flutter/material.dart';

class SohrabText extends StatelessWidget {
  const SohrabText({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Text("Sohrab text");
  }
}
